---
"rehynav": minor
---

Replace three-layer route definition with function-based API (`tab`, `stack`, `modal`, `sheet`). Breaking change: Screen component removed, old RouteMap types removed, createRouter signature changed.
